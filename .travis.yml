sudo: false
language: node_js
os: linux
install:
  - yarn
notifications:
  email: false
branches:
  only:
    - master
cache:
  yarn: true
  directories:
    - $HOME/.yarn-cache
    - node_modules
script:
  - yarn build
  - yarn test:ci
after_success:
  - CODECLIMATE_REPO_TOKEN=$CODECLIMATE_REPO_TOKEN npx codeclimate-test-reporter < ./coverage/lcov.info
