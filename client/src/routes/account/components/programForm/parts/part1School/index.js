import React from 'react'
import IndexedPartLayout from './../../indexedPartLayout';

const Part1School = ({ index, totalIndex, }) => {
  return (
    <IndexedPartLayout index={index} totalIndex={totalIndex}>
      <div>
        [field]
      </div>
    </IndexedPartLayout>
  )
};

export default Part1School;
